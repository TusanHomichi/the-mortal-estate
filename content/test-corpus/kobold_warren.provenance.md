# Kobold Warren Fixture Provenance

`kobold_warren.json` is clean project-authored prototype content.

Evidence basis:

- The tracked monster encounter archetypes design defines the nuisance
  forager as a named deterministic behavior (chase while healthy, flee when
  wounded below half hp, fielded in packs).
- Behavior direction was informed by evidence-ladder tier-1 official-era
  manual creature descriptions, consumed as normalized design judgment, not
  copied text or data.
- Tracked research boundaries allow common creature vocabulary and
  independently authored homages while forbidding copied source expression,
  data, maps, names, and packet schemas.

Clean authorship:

- The map layout is an original 9x5 prototype warren authored to prove pack
  pressure and flee behavior.
- Terrain names, glyphs, movement costs, actor names, item names, stats, and
  the script sequence are project-authored fixture values.
- `Kobold` is common fantasy vocabulary; the individual scrounger, skulker,
  and runt names are original project labels, not roster decisions.

Boundary:

- This file does not copy or approximate private geography, coordinate sets,
  private numeric tables, source names, source text, source art, packet
  schemas, or ignored runtime data.
- It does not add loot, poison, status effects, creature spellcasting,
  services, NPCs, doors, stairs, client UI, networking, or persistence.
