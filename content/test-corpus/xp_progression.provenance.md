# XP Progression Provenance

- Evidence category: project-authored progression validation fixture.
- Review reference: Slice AS, Level XP, Combat Adds, and Award Events V1.
- Boundary: names, text, layout, XP values, thresholds, and resource-growth
  values are original conservative prototype choices. Combat-add cadence is
  a successor-owned initial alignment to a class-separated cumulative
  schedule, represented in clean content as sparse per-level deltas. It is
  not claimed as parity with any external release.

This record covers the root simulation scenario and its referenced world
template, simulation seed, and selected catalog profile.
