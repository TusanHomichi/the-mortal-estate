# Agent entry stub

The canonical agent guide is `AGENTS.md`. This stub exists only so tools that do
not load `AGENTS.md` automatically still receive it. It adds no rule of its own.
Edit `AGENTS.md`, not this file.

@AGENTS.md
