/** Local preview timing, independent of the authoritative server pulse. */
export const WALK_MOVE_SECONDS = 3;

/** Disposable route allowance; not settled gameplay tuning under D2. */
export const WALK_MAX_ROUTE_SQUARES = 3;
