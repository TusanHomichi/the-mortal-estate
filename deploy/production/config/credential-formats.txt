# Each value below is the entire content of the named credential file. URL,
# webhook, and smoke files are root:root mode 0400. Libpq password files are
# root:root mode 0600. Every file contains exactly one non-placeholder record,
# ends in exactly one LF, is at most 8 KiB, and contains no CR or NUL byte.
# Extra links, missing/final extra newlines, and extra lines are rejected.
# Replace REQUIRED_PASSWORD without changing the percent-encoded Unix-socket host.

database-url
postgresql://tme_runtime:REQUIRED_PASSWORD@%2Fvar%2Frun%2Fpostgresql/tme

auth-database-url
postgresql://tme_auth:REQUIRED_PASSWORD@%2Fvar%2Frun%2Fpostgresql/tme

migrator-database-url
postgresql://tme_migrator:REQUIRED_PASSWORD@%2Fvar%2Frun%2Fpostgresql/tme?options=-c%20role%3Dtme_owner

migrator-pgpass
localhost:5432:tme:tme_migrator:REQUIRED_PASSWORD

monitor-pgpass
localhost:5432:tme:tme_monitor:REQUIRED_PASSWORD

smoke-username-one
REQUIRED_PROOF_USERNAME_ONE

smoke-password-one
REQUIRED_PROOF_PASSWORD_ONE

smoke-username-two
REQUIRED_PROOF_USERNAME_TWO

smoke-password-two
REQUIRED_PROOF_PASSWORD_TWO

webhook-url
https://REQUIRED_ALERT_RECEIVER
