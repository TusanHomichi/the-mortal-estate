---
last_updated: 2026-09-13
revision: 2
status: Ordinary control delivered through PR 75; immediate fire return in verification, with further exceptional routes still open.
public_safe: true
summary: Ordinary return delivery, immediate fire continuation, backlog reconciliation and remaining death and lineage work.
---

# Death, return and continuity

## Dispatch and owners

The owner accepted backlog reconciliation, a complete life–death–return loop,
then succession and persistent ancestors. This resumes the corresponding gameplay
work in the [genesis ledger](genesis-ledger.md). The separate presentation
experiment, visual acceptance and public-release gates remain with the owner.
Base: `a3feafc0e0fd7a334a3030845ef44f3152f4bdf4`.

The [gameplay baseline](../gameplay-baseline.md#ordinary-death-and-return) owns
historical correspondence and the new corpse-bound ruling. The
[boundary map](../boundary-map.md#22-death-as-continued-play) owns death, lineage,
succession and memory seams. The
[surface brief](2026-09-05-first-land-surface.md#living-town-dead-town-and-dungeon)
owns corresponding dead-town geography. Rules own transitions; validated content
owns destinations and adjustable values; the server owns durable application;
the browser presents the observed result.

The August packet is design history to reconcile. Individual deadlines supersede
its shared pulse, the historical baseline supersedes blanket fresh-value design,
and current authored geography supersedes the proof settlement. Its numerical
proposals do not become historical facts through earlier design approval.

## Execution order

1. Reconcile open issues against active rendering and retained reference tools.
   Correct the independently scoped staged-whitespace verification gap.
2. Ordinary death: preserve awareness and communication, expose authoritative
   resurrection eligibility, refuse physical actions and prove death/reconnect/
   return through the real browser and persistent server.
3. Author and prove the corresponding dead town and its entry conditions, then
   complete distinct rescue and earned-return routes. Strengthen populated
   spatial-state migration proof before terrain-changing installation.
4. Reconcile and implement explicit succession, inheritance and ancestors with
   one durable transaction and observer-filtered memory.

Each slice receives causal tests, native proof where applicable, configured full
verification, authorized Git delivery, preserved-save preview activation and
cleanup. Completion of a slice is not completion of the programme. Unknown
numerical values and unresolved product choices remain explicit.

## Initial findings and proof inventory

- `engine/timing.rs` rejects every nonliving actor intent; command assessment
  separately returns `ActorNotLiving` for every command.
- `engine/visibility.rs` reduces nonliving observation to the current square.
  The observer actor list filters by location alone, exposing other ghosts.
  Actor visibility, inspect targets and event projections need one rule.
- Typed social messages exist on the server, but the playable browser exposes
  no speech controls. Integrate the existing path.
- Corpse creation and transactional resurrection exist. The no-corpse defeat
  branch emits a request without completing return. Reconnect cannot supply a
  replacement recovery policy.

Search `ActorLifeState`, `is_alive`, `ActorNotLiving`, `actor_can_act`,
`ResurrectionRequest`, `SocialBroadcastScope`, wire/observer versions, checkpoint
validation and browser command gates. Reconcile every changed caller, fixture,
golden and retired roaming-ghost assertion atomically. Final proof and delivery
outcomes belong to the slice receipt.

## Backlog reconciliation

This is the September 13 classification of the open issue index. Classification
does not close an issue or waive its acceptance criteria.

| Issue | Current disposition |
| --- | --- |
| [71](https://github.com/TusanHomichi/the-mortal-estate/issues/71) | In this slice: independent staged and unstaged whitespace checks, including cancellation between them. |
| [73](https://github.com/TusanHomichi/the-mortal-estate/issues/73) | Next death slice: exceptional routes, dead-world entry, rescue and attrition; ordinary automatic timing waits for evidence by owner ruling. |
| [74](https://github.com/TusanHomichi/the-mortal-estate/issues/74) | Authored first-land balance gap: the starting scavenger cannot hit current players; recover appropriate values and prove migration consequences separately. |
| [56](https://github.com/TusanHomichi/the-mortal-estate/issues/56) | Active test-toolchain maintenance; separate dependency migration and audit proof. |
| [54](https://github.com/TusanHomichi/the-mortal-estate/issues/54) | Required before a terrain-changing saved-world migration; this slice changes no existing terrain or coordinates. |
| [50](https://github.com/TusanHomichi/the-mortal-estate/issues/50) | Active temple-exit usability requirement; prior pixel viewport evidence needs reassessment in current 3D. |
| [48](https://github.com/TusanHomichi/the-mortal-estate/issues/48) | Coastal visual and device acceptance remains open; earlier shader measurements describe the retained study renderer. |
| [29](https://github.com/TusanHomichi/the-mortal-estate/issues/29), [21](https://github.com/TusanHomichi/the-mortal-estate/issues/21), [19](https://github.com/TusanHomichi/the-mortal-estate/issues/19) | Retained reference-tool defects, routed by the browser reference owner; not blockers for ordinary death control. |
| [10](https://github.com/TusanHomichi/the-mortal-estate/issues/10) | Separate presentation experiment remains paused; no lab retirement authorized by this dispatch. |
| [7](https://github.com/TusanHomichi/the-mortal-estate/issues/7) | Identity-cleanup follow-up; local source search found no old organization spelling, but the broader automation/history-link audit remains unproven. |

## Ordinary-death implementation

The first slice adds a parameterless request through the existing gameplay
transaction and social speech through the existing independent message envelope.
Rules derive the request deadline from `Ghost.defeated_at` and the realm policy;
the scheduler publishes eligibility without automatically returning the actor.
An accepted return uses the existing corpse/inventory transaction and schedules a
full standard cooldown. Recovery must retain the matching corpse, location and
death timestamp. Living observers receive corpses, not invisible ghost actors;
the controlled ghost retains normal radius, occlusion and speech recipients.

World-template V4 requires explicit realm policies. The first-expedition
declaration selects lawful temple `(3,6)` and neutral outdoor dock `(9,31)`
destinations. Current-pool deficits remain the existing restoration convention of
one HP and one stamina below maximum. These are provisional implementation values,
not a claim of recovered attrition odds or permanent penalties. Full attrition,
corpse custody, eaten routing, dead-world entry and lineage remain subsequent
work. The fire continuation is recorded below. An empty fixture map explicitly
authors no return route.

This is an atomic content-schema cutover. Normal loaders refuse V3. The saved
preview requires a backed-up, stopped-writer offline migration with pinned source
and destination validators and exact checkpoint conservation, followed by recovery
and rollback proof. No player, item, balance, terrain or location is reseeded.
Wire minor 11 and observer/command contracts move with every caller and proof.

Wire refusal mutants exposed serde's internally tagged unit-variant behavior:
extra fields were discarded despite the strict annotation. The parameterless
`Intent` variants now use empty struct variants, preserving the accepted JSON
shape while refusing every extra key. Each has a shared native/WASM corpus
mutant. The externally tagged internal command payload retains its existing
shape. The command conversion was split from assessment by responsibility.

The repeatable direct/simulation/facet oracle now binds
`a16de5ece6b7f1525a3a905150616fc59200b6e51b215e5deb41f59a9ac2a248`.
Its input contains complete checkpoints and observer projections, so the content
identity and contract cutover intentionally replace the previous digest. Every
direct/facet byte-comparison and repeated-run comparison passed before the new
pin. The maintained V2 golden traces were regenerated and independently compared:
only contract versions and removal of dead observed actors changed; V1 is
unchanged. The certification file's ownership remains deterministic direct/facet
comparison; its changes are mechanical intent shapes, version and digest pins.

## Verification and delivery evidence

Focused parent-observed results: 27 whitespace/target tests, 15 death/corpse rules
tests and 38 browser speech, death-control, path and figure tests passed. Authoring
regenerated all three lands successfully. Final configured verification, native
matrix, source and saved-preview delivery outcomes belong to
[PR #75](https://github.com/TusanHomichi/the-mortal-estate/pull/75) and installation
receipts; initial focused results do not establish them.

The native runner is `tools/run_death_return_proof.py --release <release>
--admin-url-file <private-file> --output <private-directory>`. It owns six
disposable cases: lawful male and neutral female in each configured browser.
Real movement joins a hold-ground adversary, production combat causes death, and
the browser exercises speech, reconnect, eligibility, request, cooldown and
living movement. It does not synthesize death events or change a running world's
state to obtain a pass. Source-validator build and explicit schema-cutover
rehearsal artifacts remain outside the checkout.

The configured full run exposed one remaining storage assertion at the retired
action-context version and the Python smoke/live-wire clients' retired protocol
and observer pins. All callers were migrated; the smoke error now formats its
selected version. The Rust workspace, 629 browser tests and seven real PostgreSQL
cases passed before the restore drill exposed that final client pin. Failed runs
remain evidence, not complete verification.

Initial native inspection caught the 3D layout hiding diagnostic movement and
action controls and covering the new speech form with its full-screen canvas.
The proof now uses real movement keys; the playable layout has visible speech and
death panels, with request dispatch bound to the current authoritative offer.
The first candidate passed the saved-state migration rehearsal, full checkpoint
conservation, restored startup and exact-row rollback on a disposable installation.

The initial lawful Chromium case passed real death, speech, reconnect, the
authoritative request, retained inventory, temple return and living movement.
The adversary fixture explicitly sets attack to 80 and disables scavenging; both
changes and source/fixture catalog hashes are in its receipt. The first-land
opponent cannot hit the starting player under its current authored scores (#74),
and a looting opponent legitimately removes corpse contents during the wait.
This fixture isolates return and inventory restoration. It changes no production
combat resolver or installed catalog and proves no claim about initial balance.


## Immediate fire-return continuation

Base: `b7d6d74678b6092e06abb937c49c8b8a546e3c90`. The owner continued the programme
with its already approved fire exception. The gameplay baseline owns alignment,
drops and immediacy; this slice completes lawful/neutral fire return with an
authored policy and supported current resources. The no-corpse state is internal
to the lethal transaction and no ghost interval is published. Non-fire no-corpse
cases, absent policies and unsupported alignments/resources retain their explicit
awaiting state; issue #73 retains their completion criteria, including the
pre-existing max-HP-one restoration floor conflict. No ordinary automatic timer
is added. No content, wire or checkpoint shape changes.

Hazard proof checks same-instant return, item/gold conservation at the death site,
no corpse allocation, one resurrection, checkpoint recovery and complete cooldown.
The native fire mode uses an explicit disposable monster-ability fixture and the
same configured six browser/body/route cases. Its catalog digests and overrides
are retained. A self-target direct-damage fixture was rejected because that target
executes as stubbed; the accepted proof uses the existing automatic monster spell
path. Self-target damage support is tracked in
[issue #76](https://github.com/TusanHomichi/the-mortal-estate/issues/76), separately
from the fire route. Final configured verification and saved-preview activation
receipts must establish delivery before this continuation is marked complete.


The parent observed all six native fire cases pass on chromium, firefox and
webkit, with lawful male and neutral female bodies. The rules test fixes the
cooldown at exactly three seconds from lethal resolution. Native publication may
follow a scheduled death by a small elapsed interval, so its assertion checks a
positive remaining interval no greater than three seconds and an unchanged
deadline after reconnect; it does not mislabel publication time as damage time.
The first native attempt exposed and corrected that proof-clock assumption.
