---
last_updated: 2026-09-13
revision: 2
status: Proof hardening implemented with configured full verification; PR and installation receipts own delivery and activation outcomes.
public_safe: true
summary: Close the restore-drill delivery, shared teardown and block assertions, then rehearse and activate the merged martial gameplay with preserved saves.
---

# Proof and private-preview closeout

## Dispatch and ownership

The owner requested the September 12 resumption assessment's immediate work
end to end, including cleanup. This dispatch authorizes implementation, full
verification, PR review and merge, private-preview refresh, and branch and owned
scratch-resource cleanup. It continues PR #67 from `2739a8a` on `b11306e` and
includes issues #49, #60 and #66 in one final delivery.

This is a Planning execution record. The [server owner](../server-notes.md)
owns saved-state preservation and activation; the
[development runbook](../../deploy/development/README.md) owns operations;
[verification](../verification.md) owns capability and lifecycle proof; the
[browser owner](../browser-client.md) owns native presentation evidence.
The [restore record](2026-09-11-restore-drill-preservation.md) retains the earlier
branch's implementation and observed receipts.

No gameplay formula, timing, authored geometry, asset master or public-player
activation changes here. Vitest and populated terrain-migration follow-ups remain
separate backlog. The identity-proof and presentation-experiment pause remains.
Owner visual acceptance cannot be supplied by an implementation or technical pass.

## Implementation and proof

1. Declare the real private-terms prerequisite for served proofs, including the
   new restore drill. Exercise the actual step table with PostgreSQL tools present
   and private terms absent; require honest UNAVAILABLE/INCOMPLETE and no launch.
2. Make shared live-server cleanup bounded, confirmed and observable. Preserve a
   primary proof error, retain useful diagnostic roots on failure, retry unresolved
   obligations, and never drop a caller-owned installation. Cover failed/timed-out
   DROP, forced termination and reaping, keep mode and combined failures.
3. Separate scratch installation/provisioning from restore proof orchestration:
   the new proof launcher is over 1,000 lines. Migrate imports and failure-injection
   tests together. Report root-deletion failures after confirmed cluster shutdown.
4. Assert stationary motion on both sides of each block screenshot; collect the
   occupied-hand control's final counts and zero-command assertion after capture.
   Preserve the documented bracketed-observation limit.
5. Run the selected fast plan and configured full baseline, plus native martial
   movement and defender-block scenarios across the shared browser roster. Review
   captures and report remaining visual findings without granting acceptance.
6. Stage the verified candidate and rehearse the exact before/after content cutover
   on an isolated copy while CI runs. Review and merge the final PR with both
   required CI jobs successful. Require the merged tree to match the proven release,
   preserve all player state, back up with writers stopped, install by
   compare-and-swap, restart and prove the installed release. Retain recovery
   material and remove only this delivery's unneeded branches and scratch resources.

Blast-radius searches: `rg -n 'LiveServer|server\\.close' tools tests`,
`rg -n 'ScratchInstallation|postgres_binaries|LAUNCH_ATTEMPTED' tools tests`, and
`rg -n 'gated.restore_drill|private-terms|capture.authoritative' tools tests docs`.
Inspect the runner's resolved plans before executing them. Freeze source during
integrity proof; run only one full verification on this host.

## Findings and receipts

The entry-state CI failure is confirmed in run `34663703561`: PostgreSQL server
tools were present but the private denylist was absent; the new proof launched
and failed because its declared prerequisites were incomplete. The previous local
full pass is historical evidence, not proof of these follow-up changes.

The implementation receipt below records source verification. Native capture,
Git delivery, installed-preview preservation and cleanup outcomes are recorded on
[PR #67](https://github.com/TusanHomichi/the-mortal-estate/pull/67), with the private
installation's immutable release, migration and activation receipts owning exact
installed state. Those receipts bind the complete checkpoint comparison (only the
content definition digest may change), every account and character row, stopped
writers, backup, compare-and-swap, restart and rehearsed rollback. Machine paths,
credentials and private captures remain outside the checkout. A source merge alone
does not establish deployment or visual acceptance.

The final lifecycle review also closed snapshot-client retry and proof lock-holder
error-reporting gaps. An unconfirmed drill database creation now names the resource
for inspection without claiming ownership; the runbook owns that recovery contract.
The scratch release records a tree identity separately from its base commit.

The first follow-up fast run was interrupted by host resource exhaustion and an
owner-performed reboot. It is not a verification receipt. The replacement run
keeps its logs on persistent storage and uses a separately bounded process group.
The source of the host exhaustion was not established; no repository defect is
claimed from that incident alone.

Configured `python3 tools/run_verification.py --scope full --report-disk` completed
on the staged source tree `3849fad18c792221d892c2394f9445d9b132b14b`:
**COMPLETE**, every selected step passed, 946.819 seconds. It included 659 Python
tests, the Rust workspace and doctests, 604 browser unit tests, all gated PostgreSQL
cases, the real restore drill (29.224 seconds), trusted wire proof, the full browser
capture roster and clean-clone proof (382.675 seconds). Every required capability
was supplied locally. The interrupted fast run contributes no pass claim; this full
run supersedes its selected coverage. The only subsequent source-tree edits are
these documentation receipts, verified through their resolved documentation lane.

The candidate's bounded native matrix is both martial bodies plus male/female
defense and the occupied-hand control on chromium, firefox and webkit: 15 cases.
The installed check visits each saved character, renders, reconnects and signs out
with zero gameplay commands. The PR carries the observed outcomes; technical
passes leave exact kick phase alignment and artwork acceptance with the owner.
