---
last_updated: 2026-09-08
revision: 15
status: Final temple benchmark selected by owner; subsequent pixel-art transition owns production cutover.
public_safe: true
summary: Historical experiment and selected benchmark evidence; production continuation routes to the pixel-art transition.
---

# Playable pixel temple

**Subsequent owner ruling:** the final result below is the selected benchmark.
The [pixel-art transition](2026-09-08-pixel-art-transition.md) supersedes the
initial separate-experiment scope and retired build command recorded here.
The following scope and iterations are historical evidence.

The owner requested a separate playable pixel-art temple on September 8,
following the richer-palette guide studies. This authorizes a bounded Canvas 2D
presentation experiment and use of the existing PixelLab allowance alongside
built-in image generation. At that point Three.js remained the production direction; the initial experiment
selected no replacement renderer or accepted artwork master.

The study shares `PlayControl`, Rust wire decoding, path controls, resident
interaction and service offers. It owns only image loading, pixel projection,
sprite drawing and semantic picking. Movement, resident circuits, stock,
readiness and descent remain server facts. There is no AI at runtime.

The explicit `pixel-temple` build has its own output and external art packet.
The packet manifest and every image are digest-bound; its geography binding
must match the current authored promotion. Missing or stale assets refuse the
study. Generated art and provenance remain outside the checkout under the
existing candidate-art exception. The room has no baked residents. A fixed
projection maps the authored 7-by-8 temple to the illustration; observed actor
rows alone create sprites. Other locations use an explicitly labelled map
presentation so the real descent and return remain usable within this bounded
temple art study.

## Execution and proof

Search scope: `rg 'TME_PLAY_PRESENTATION|ExpeditionStudyRenderer|presentWalk' web`
and the renderer, asset, browser-proof and local-server owners. Preserve the
concurrent town/content work; do not publish or change a running preview.

Implement the separate renderer and selection, external packet receipt, local
scratch-world launcher and browser proof. Start the existing seeded player in
the temple through the harness's explicit generated seed; do not alter tracked
content or any persistent product world. Prove malformed/stale packet refusal,
observed-only picking, reconciliation and cleanup; then exercise actual movement,
resident offers, balm purchase, descent and return in Chromium, Firefox and
WebKit. Capture native frames and report measured rendering cost separately from
unmeasured production performance. Run the verification runner's selected plan.

## Open visual findings

The generated guide is an illustration, not an exact map. Furniture alignment,
sprite proportions and foreground occlusion require native review. The client
owner must assess the captured playable result before any production adoption.

## Execution receipt

Implemented against `10a8800869a03ede0441b34a6e054d2fb499cbdb` on `main`, with
pre-existing uncommitted town/content work preserved. No Git lifecycle,
publication or running 3D-preview change was performed. The external
`pixel-temple-20260908-r1` packet contains the untouched generated background,
120 RGBA sprite PNGs, exact prompts, source receipts, digest manifest and alpha
inspection. Three v3 characters plus four-direction eight-frame walks consumed
21 PixelLab allowance generations; 4,399 remained. No Retro Diffusion credit was
spent. The background is drawn into a native 512-square canvas with smoothing
disabled; figures are independently anchored, depth-sorted and animated only
after authoritative position changes. Stable idle scenes do not redraw.

The native `--proof` run passed Chromium, Firefox and WebKit on separate scratch
databases. Each exercised thirteen accepted commands: footprint movement,
Tomas's moving service location, Maude's balm purchase, descent, return and
reconnect. Opening a resident and drafting a path sent no action. The proof also
verified cooldown input gating, sign-out clearing pixels and actors, pinned
build selection, absence of GLB/GLTF loads, and startup refusal for a changed
manifest digest or missing room image. Exact reports and captures are in the
packet's `evidence/matrix/`. This establishes local browser behavior, not Safari
version, packaged Tauri or remote production support.

The first native run exposed a same-frame clock ordering defect: a queued rAF
timestamp could precede the snapshot's `performance.now()` and select a negative
animation frame. Clamping elapsed presentation time fixes it; the regression is
`pixelPacket.test.ts`'s same-frame network-update case. A later proof assertion
queried disabled state on a fieldset instead of its sign-in button; the corrected
mutation checks passed in all three engines. Failed inspection artifacts remain
external and are not successful receipts.

The initial selected verification plan passed all web, Python, boundary and docs
steps (49 browser unit-test files, 521 tests). The subsequent clock regression,
dynamic diagnostic import, owner documentation and refusal proof receive a
final selected run recorded beside that first log. No full/merge proof was
requested; the selected run does not claim to certify unrelated dirty Rust work.

## Findings retained for visual review

- Presentation owner: PixelLab's character proportions are more cartoonish than
  the room guide. Source/native light-and-dark composites passed alpha inspection;
  anatomy and palette matching still require owner judgment against native
  captures. Passing alpha is not artwork acceptance.
- Presentation owner: only the temple has finished study artwork. The labelled
  map beyond it is sufficient to exercise real descent/return; dungeon and town
  art are separate future work.
- Browser client owner: draw-duration measurements cover this Canvas scene only.
  They exclude a controlled 3D comparison, full game frame latency, cold network
  loading and target-device certification. The candidate uses individual sprite
  files; production asset packing is unmeasured.

The separate local launcher and the recorded walking clip provide reviewable
output. An accepted master, production renderer choice and any external preview
publication remain owner decisions.

## Floor and character correction

The owner identified competing painted-floor and movement grids, and requested
less cutesy characters. The cutesy 3D proportions had partly concealed modeling
limitations; that constraint need not carry into this separate pixel study.
The revised room plate removes regular paving joints while retaining worn
material texture. Runtime grid edges, hover, route outlines and pointer targets
share one projection and cell bounds. Grid geometry comes only from observed
passable tiles, draws shared edges once and sits beneath actors. It is independent
of the illustration. Furniture and occlusion still require visual calibration;
removing painted tiles does not by itself prove an entire image matches collision.
The owner further required the same room grid for all players and creatures.
Observer visibility selects a subset of the server's squares; it does not
renumber, offset or resize those squares. The overlay has no actor or observer
input, and all actor kinds use the same coordinate projection. A visibility-
subset test proves that two observers retain identical shared cell boundaries.

Revise the existing outfits and identities toward adult limb/head proportions,
smaller eyes, worn cloth and restrained faces, then regenerate rotations and
walk cycles. Keep their gameplay display height stable while admitting the
more detailed sprite canvas. These remain review candidates. Update the external
packet and its pinned receipt atomically; prove the grid/picking boundary and
inspect the playable result and source/native alpha composites before closeout.
The provider returned different source sizes and padding. Packet version 2
therefore requires measured visible `body_height` and a lower-body sole anchor
for every rotation and walk frame; the renderer scales the body to 72 native
pixels in the temple. Empty source padding cannot move the displayed ground
anchor. Every packet producer and fixture migrates
together, and the retired version 1 is refused. The existing R1 preview retains
its previously built renderer and packet until the replacement is reviewable.

The revised external `pixel-temple-20260908-r2` packet contains the plain-floor
plate and 120 new RGBA sprites. Source/native composites were inspected against
light and dark backgrounds. Furniture patches hide the separate ground overlay
and sprites behind foreground objects; these patches carry no passability.
The source PNGs remain unchanged, with per-frame calibration stored in the
digest-bound version 2 manifest. Provider retries and rejected proportions used
134 allowance generations; 4,265 remained. No Retro Diffusion credit was used.

Presentation owner finding: the traveler and Maude now have more adult
proportions, but Tomas retains a larger head and flatter shading than the other
two. Native captures in this packet are the evidence for the next visual review;
a coherent accepted character style remains unproved. A successful browser
test does not close that artwork finding. The provider also failed one traveler
rotation job without producing frames; its successful replacement has complete
directions and walking cycles. The external generation record retains both IDs.

R2 passed the thirteen-command native loop in Chromium, Firefox and WebKit,
including independent grid ink and furniture occlusion pixel checks. Its
walking clip and local preview preserve that review checkpoint.

## Character resolution correction

The owner rejected Tomas's remaining cartoon proportions and observed that
characters looked coarser than their surroundings. R3 redraws detailed character
references with natural adult proportions before
generating 256-square directional sprites and walks. The room geometry is retained.
The renderer doubles its backing raster from 512 to 1024 while preserving the
512-unit art calibration, CSS display size, room coordinates, pointers and
foreground rectangles. A 72-unit body now receives 144 raster pixels. This
removes the low-resolution intermediate that previously discarded sprite detail;
it does not enlarge characters relative to their room or alter the shared grid.
Source assets and generation provenance remain in the separate R3 packet.

The owner then rejected Christian crosses and church-like presentation and
directed use of the existing open-hand symbol. The current external temple and
world briefs and original faith/character profiles were inspected. The resulting
visual ruling belongs to presentation direction; the external receipt retains
source identities. Tomas's first R3 vestment candidate and its pending walks
were retired. A character-state edit preserves his finer adult anatomy while
restoring young-adult features, simple working robes and one open-palm emblem.
The room edit removes cross motifs from the altar without moving its silhouette
or other furniture. No lore payload becomes a build input, and no unresolved
religious origin or draft practice becomes a new gameplay fact.

The first 256-pixel template walking sequence failed visual inspection: the
traveler's cloak and outfit changed across frames, and a north-facing sequence
contained a front-facing pose. Pending template jobs were cancelled. Their
receipts and rejected frames remain external evidence. Custom v3 walks preserve
the inspected reference pose and generate four walking frames per direction;
the packet selects one explicit animation group for each figure, excluding
rejected provider groups. This is an authored asset selection, with no runtime
fallback to the rejected frames. Fine-resolution animation quality must be
inspected independently of successful generation and alpha validation.

After the first R3 native capture, the owner found the figures too small against
the bed and counters. Visible body height increases from 72 to 96 logical units
(192 backing pixels, 144 CSS pixels at the 768-square presentation). This is a
one-third increase with the same per-frame sole anchor, shared room cells and
furniture. The anchor regression now proves both display heights. The earlier
R3 capture remains historical evidence, not the final scale receipt.

## R3 closeout

The final external packet contains the open-hand room plate and 84 untouched
256-square RGBA sprite PNGs: eight standing directions and four five-frame
walking atlases per figure. The pinned manifest ends in `920fa304`. All PNG
digests and alpha bounds were checked; final contrast sheets, rotations,
walking contacts and native room captures were inspected. The four-direction
19.6-second browser clip returns the player to the original square. The separate
local preview serves the final 96-unit character height.

The final-scale native matrix passed Chromium, Firefox and WebKit. Each ran
the thirteen-command movement, resident-service, purchase, descent/return and
reconnect loop, with startup refusal and logout clearing checks. The backing
raster is 1024 square; independent grid ink and furniture occlusion checks pass
at that resolution. Earlier smaller-scale captures and rejected animation
results remain explicitly separate historical evidence. The selected fast
verification plan is recorded in the packet and receives a final run after
this closeout; its observed result is recorded in the external provenance.

R3 used 178 PixelLab allowance generations, including rejected and cancelled
work; 4,087 remained. Built-in image generation supplied the detailed references
and room correction. No Retro Diffusion credit was used. Public source contains
no generated imagery, provider credential or private lore payload. Ancestry
remains `10a8800869a03ede0441b34a6e054d2fb499cbdb` with concurrent dirty work
preserved; no Git lifecycle, production adoption or external publication occurred.

Presentation-owner review remains necessary for an accepted master. The front
walking cycles have a restrained stride, and Tomas's plain robe shading is
simpler than the other two figures. The native clip and final contact sheets
are the proof for any subsequent refinement. Read current character and faith
briefs before prompting; a generic priest prompt introduced the rejected
Christian imagery and incorrect age. Generation completion and transparent
alpha alone did not prove directional or clothing consistency.

## Remote owner access

The owner clarified that this checkout runs on a remote development machine;
loopback links cannot provide the requested playable review. This authorizes a
separate owner-accessible HTTPS study endpoint. The launcher now accepts an
explicit `--public-origin`, supplied to server host/origin admission through the
shared harness. Invalid origins fail before provisioning. Loopback remains the
only upstream listener; a separate host proxy verifies its TLS certificate and
passes the real remote host and origin without rewriting admission identity.

The host configuration, credentials and remote browser evidence remain in the
external study packet. The established domain certificate serves a separate TLS
port. Sign-in gates gameplay; enrollment and operations are unavailable. The
existing 3D preview retains its routes and persistent world. This study remains
disposable and independently stoppable, with no Git lifecycle or artwork-master
promotion. Remove its separate proxy configuration and reload nginx before
stopping the launcher, which drops only its scratch world.

Remote Chromium proof passed thirteen commands, resident services, purchase,
descent/return, reconnect, logout and asset-refusal checks with normal certificate
validation. The remote endpoint refused a foreign login origin (403) and private
operations/credential paths (404). The selected Python, boundary and docs runner
completed: 552 Python tests passed. Exact logs and captures are under
`evidence/remote/` and `evidence/verification-remote.log` in the external packet.
The earlier loopback-only handoff was an access mistake; future review links must
name the tested remote origin when the owner's machine is elsewhere.

## Frontal character art and grounded locomotion

The owner accepted a bounded traveler polish after the native walkaround:
match the elevated room camera and warm light, then improve the directional gait.
Keep the adult display scale, room art, other two character assets and shared
room grid. The prior candidate remains available for comparison. This slice
includes the cause of the observed glide: a command receipt may precede its
position frame and clear the committed route against the old ready snapshot.
Preserve that route through the handoff, then prove the traveled corners and
distance-based gait in the pixel renderer. Server deadlines remain unchanged.

The separate external R4 packet owns exact image prompts, provider receipts,
alpha/direction inspections and native comparisons. Camera/light changes must
be assessed in the actual room before selecting the packet. The static-room,
crowded-occupancy and grid-emphasis findings from the walkaround remain outside
this one-character slice; presentation owns their later native proof.

The owner rejected the elevated candidate and then preferred the original art
and palette. The subsequent clarification extends frontal readability to
creatures and building facades. Restore the original traveler rotations and
retain their camera angle in any new walk frames. The elevated/shading branch is
superseded; its packet and proof remain external historical evidence, not the
selected result. The current slice now completes grounded locomotion with the
earlier appearance. No changes to the separate 3D preview are dispatched.
The owner further clarified that frontal readability must apply to all upright
assets, explicitly including trees; presentation direction owns that shared rule.


The selected R4 packet preserves the R3 room, every original standing sprite and
anchor, and all Tomas/Maude assets exactly. It adds eight frames in each of eight
traveler walk directions. A fixed reference scale preserves pose weight transfer;
stride phase follows traveled room distance. Native tracing showed that an
unrelated NPC ready frame could also arrive between the receipt and landing.
The shared path controller now retains the submitted route until displacement
or cooldown establishes the handoff. The regression proves all three squares
remain in the route and travel is not compressed into a short direct glide.

The final selected fast runner completed: 50 web test files and 530 tests passed,
with typecheck, production build, documentation and boundary checks. Chromium,
Firefox and WebKit each passed the thirteen-command native loop, including the
new route-retention assertion. Exact logs, packet digests and comparisons remain
in external R4 provenance. The elevated candidate's passing mechanical proof
remains separate from the selected art. No accepted master is claimed.

Finding for the browser-control owner: the native trace contains an old ready
snapshot after receipt settlement and before the landing frame. The current
control guard can therefore appear ready during that interval. This slice fixes
route retention; whether a second user command can exploit that interval needs
a delayed receipt/landing input proof before changing command gating. Evidence:
external R4 `evidence/motion-trace/chromium-failure.json`; owner:
[browser client](../browser-client.md), `PlayControl` command settlement. Server
legality remains authoritative; no invalid accepted action was observed.

This plan is an execution record; presentation direction alone owns the upright
asset rule, and browser client notes own the renderer and control contracts.
Work remains on dirty `main` at `10a8800869a03ede0441b34a6e054d2fb499cbdb`,
with concurrent work preserved and no Git lifecycle operation.

The existing owner-accessible pixel frontend now serves the final R4 bundle and
asset mount, with the same account, server and disposable world. Its remote
Chromium recording completed nine ordinary click-confirm moves in 41.8 seconds,
opened Tomas's interaction, returned to the starting square and signed out with
no page errors. The separate 3D preview was untouched. R4 provenance contains the
live recording, frontend configuration backup and rollback instructions.


## Walk-cycle anchor correction

The owner reported a hitch, most visible while walking toward the camera.
Frame inspection identified an asset-calibration error: each frame's lowest
opaque boot pixels supplied a fresh ground anchor. In the south cycle that
anchor moved eleven source pixels vertically, shifting the entire body as a
boot lifted. The loop boundary also shifted the body sideways. Equal frame
intervals alone could not correct this spatial discontinuity.

R5 keeps every PNG, frame order, display scale and travel interval unchanged.
Each traveler direction now uses its original standing rotation's ground pivot
for all eight walk frames. The manifest alone owns this calibration; no runtime
special case or animation regeneration was added. The native-size diagnostic
estimates the largest adjacent south head displacement at 6.61 CSS pixels before
and 2.80 after, including the loop boundary; the remaining movement belongs to
the drawn poses. Exact measurements, before/after recordings and proof remain
in the external R5 packet. Browser client notes own the pivot contract.

This bounded correction preserves the shared authoritative grid, original
frontal art and separate 3D preview. No provider allowance or Git lifecycle work
is used. The prior R4 packet remains the comparison and rollback source.


R5's selected fast runner completed with 530 tests in 50 web files, typecheck,
build, docs and boundary checks. Chromium passed the thirteen-command native
loop with packet refusal and route-retention checks. Both normal-TLS remote
recordings completed six moves, repeated the camera-facing walk three times,
returned to the same starting square and signed out with no page errors. The
comparison plays both independent captures at normal speed. The existing pixel
frontend now serves R5 with the same account and world; its configuration backup
and exact evidence paths are recorded in external provenance. This correction
has Chromium native proof; the preceding R4 three-engine run remains history.

The lesson is to inspect a walk's registered loop at play size, including the
last-to-first transition. Transparent bounds are useful for measuring padding;
they are not automatically a valid ground pivot for every animated pose.


## First reusable class concept

The owner preferred beginning reusable class artwork before further refinement
of the generic traveler, which may suit a thief. The first external unarmed-class
sheet contains the same original character standing frontally, in a guarded
three-quarter pose and from behind. It retains adult proportions and frontal
readability with a bare torso, dark cloth trousers, sash and wrist wraps. These
are candidate design choices, not requirements imposed on every class member.

Built-in image generation produced the concept. The external packet records the
exact prompt, inspected inspiration, original choices, image digest and review.
No directional sprite generation, game-preview replacement, provider allowance
spending or accepted-master promotion occurred. The generated physique is more
muscular than the requested wiry direction; owner review remains open. The sheet
is a design reference, not a finished runtime sprite or animation set.

Presentation direction owns the reusable-art priority. Classes and training owns
the accompanying class-by-class display-name review; no rename is selected.
No gameplay or identifiers changed, and concurrent work remains preserved.


## Martial Artist in-room standing review

The owner retained the Martial Artist name for now and requested seeing the
concept in game. The bounded next packet extracts the original front figure,
converts it to a transparent 256-square sprite and derives eight standing views.
It uses the same 96-unit display height and authoritative grid as the preceding
traveler. The isolated native review captures settled poses inside the real
pixel renderer and room; it is not a generated room composite.

This is standing-art review only. Each directional presentation uses one held
pose while positioning the character; no new walking or combat cycle is claimed.
The public pixel study and separate 3D preview retain their existing bundles and
worlds. Generated assets and exact prompts remain external candidates, with no
class/loadout mutation or master acceptance. Classes and training owns the
retained name. No Git lifecycle operation is dispatched.


The standing rotation inspection found an invented dark ankle strap/block in the
converted set. Class-art authoring owns removal before master acceptance or
animation expansion, with side/back and native-size bare-foot comparisons as
proof. The external rotation sheet and provenance record the finding; the
current images remain a review candidate. The selected fast runner completed
with 530 web tests, typecheck, build, documentation and boundary checks.


The isolated Chromium inspection passed startup, digest-bound image loading,
five authoritative positioning moves, settled front/side/back captures and
logout with normal TLS validation and no page errors. The character returned to
the start. This proves native standing presentation only. The external packet
owns exact captures and allowance accounting; the established previews were
not changed. The disposable inspection server is stopped after capture.


## Martial Artist ankle cleanup

The owner requested cleanup of the invented ankle hardware seen in the native
standing review. The bounded correction removes ankle devices, straps and
bindings in all eight views, leaving simple charcoal cloth hems, visible bare
ankles and bare feet. The existing hand/wrist wraps remain intentional. Preserve
the same character, adult proportions, outfit, frontal camera and room scale.
The external cleanup reference and directional state retain exact prompts and
provider receipts. Inspect every direction before closing the R2 art finding,
then repeat settled native room captures. No new gait, guard, combat animation,
class mechanics, preview cutover or master acceptance is included.


The first full-character state removed the hardware but softened unrequested
face, torso and cloth detail. It was rejected as the selected art. The corrected
method packs original standing sprites into two masked repair sheets; only the
lower-calf/ankle regions are editable. Deterministic masked assembly preserves every original pixel outside those
masks and uses generated pixels only inside them before sheet extraction and
packet assembly. The rejected state and its mechanical checks remain external history.


The provider's masked outputs still changed eight pixels outside the masks,
including stray white pixels. The final assembly therefore enforces the authored
mask locally; it is not delegated to the provider's preservation claim. Exact
RGBA comparisons passed for every original pixel outside the masks and every
generated pixel inside them. All eight repaired views were inspected, closing
the ankle-hardware finding while retaining the original upper-body artwork.


The final selected fast runner completed with 530 web tests, typecheck, build,
docs and boundary checks. Chromium then completed four authoritative positioning
moves, settled captures and logout with normal TLS validation, no page errors
and return to the start. The clean front view was inspected in the real room.
The disposable inspection launcher was stopped; established previews and worlds
remain untouched. Exact asset/mask comparisons and captures remain external.
Future animation must use the final repaired directional PNGs explicitly; neither
the original hardware-bearing provider state nor the rejected softened state
represents this selected packet. No animation or accepted-master claim is made.

## Revised Tomas standing comparison

The owner selected the recovered dark-robed Tomas as the next design starting
point, preserving his warmth and fatigue while reducing anime facial styling.
The built-in concept refines his eyes, nose and jaw, retains a lean adult body,
modest dark working robes and the open hand, and uses the Martial Artist as a
detail reference. The owner then requested a native temple trial. The existing
[presentation direction](../presentation-direction.md#the-candidate-lifecycle)
owns adult proportions, frontal upright readability and religious imagery.

The external `tomas-20260908-r4` packet owns the concept, exact prompt, provider
receipts, sprite preparation and native evidence. PixelLab removes the generated
checker backdrop and supplies eight facings. This bounded standing review holds
one pose per movement direction; the old Tomas walk art cannot carry the new
identity. A matching gait remains a separate art follow-up. Room, Martial Artist,
Maude, scale calibration and authoritative resident behavior are preserved.
No class mutation, accepted master, public deployment or existing preview cutover
is included. The native review must compare Tomas with the Martial Artist and
Maude, then stop its disposable inspection authority.

All eight facings were inspected and the packet passed the selected fast runner:
530 web tests, typecheck, build, documentation and boundary checks. Native Chromium
loaded the exact packet, captured the revised Tomas beside the Martial Artist in
front and side views, and visited Maude's corner. The direct capture click from
the cot area toward Maude did not produce a draft and timed out; its cause was
not recorded. The recovered inspection used observed intermediate floor cells,
completed four positioning commands, captured the comparison, returned to the
starting cell and signed out with no page errors and normal TLS validation.
Future capture routes must record target observability and refusal before
waiting for a draft; the external capture log owns that harness finding.

The review found compatible adult proportions and a readable dark silhouette
against the warm floor; the reduced front face retains a gentle expression.
These are agent observations, not owner acceptance. The external packet records
nine PixelLab allowance generations spent and 3,761 remaining. Its standing-only
Tomas must gain a matching gait before an animation-quality claim. The disposable
inspection authority was stopped; established previews remain unchanged.

## Character detail and scene integration

The owner observed faces breaking down and dark character outlines standing
apart from the room. The next bounded study compares unfiltered reduction,
filtered sprites, normal CSS resampling and a larger desktop room, retaining
the existing room-relative character height. Browser client owns the sampling
and responsive-layout behavior; this execution record owns comparison evidence.
The external `pixel-sampling-20260908-r1` study distinguishes controlled browser
rendering comparisons from native gameplay captures. The existing detailed
source sprites are sufficient to test the rendering cause without increasing
their source resolution. The `tomas-20260908-r5` art challenger separately tests
local-material colored edges and restrained reflected light in place of the
heavy black silhouette. Generated outputs must pass alpha and native light/dark
field inspection before packet selection; the untouched concept remains
disposable if it contains a painted checker background. Proportions, feet, grid,
costume, warm tired character and open-hand imagery remain the existing target.

The first outline challenger produced an orange rim and was rejected. A neutral
front-lit revision retained charcoal/brown material edges without that rim.
Both untouched built-in concepts contained a painted checker and remained
disposable; the extracted candidates passed real-alpha checks. All eight final
facings were inspected over light and dark fields at native character height.
The final packet retains the room, Martial Artist and Maude bytes and changes
only Tomas's standing art. Ten allowance generations were spent, leaving 3,751.

The selected final fast runner passed 530 web tests, typecheck, build, docs and
boundary checks. The full interaction matrix used the unchanged R4 art with the
new reduction/layout to isolate rendering changes: Chromium, Firefox and WebKit
each passed 13 commands, resident interaction, purchase, descent/return,
reconnect, refusal and cleanup. Desktop room width measured 1024 CSS pixels;
compact width measured 846. Compact pointing selected the same authored square
without submitting movement. Recorded draw-call p95 was approximately 1.1, 2
and 14 ms respectively; these are local observations, not device performance
guarantees.

A subsequent external-border correction removes the two-pixel content reduction
at full desktop width. Final R5 art and the exact border correction were then
inspected in all three browsers: backing and content dimensions both measured
1024 square, with normal TLS and no page errors. Chromium completed the three
positioning moves for the side-by-side front capture; WebKit restored the start.
The final crop comparison preserves source screenshot pixels without rescaling:
approximately 144-pixel characters in the old 768-pixel room versus 192 in the
new 1024-pixel room. Character-to-furniture scale and shared cell coordinates
remain fixed. The outline is less intrusive and faces read better, but fine
expression remains limited at small display sizes. Standing-only character
animation remains an open art task. Disposable inspection authorities were
stopped, private access files removed, and established previews left unchanged.
