---
last_updated: 2026-09-09
revision: 8
status: Connected town, compact-hatch temple and revised Martial Artist integrated; three-browser functional proof passed, performance follow-up open.
public_safe: true
summary: Connected town integration, bounded geography amendment, temple/character corrections and pixel-effect proof.
---

# Pixel exterior construction

This Planning and Execution record owns the bounded exterior slice. The
[presentation owner](../presentation-direction.md) and
[production guide](../pixel-art-production.md) retain aesthetic and production
authority; [browser client](../browser-client.md) owns implemented presentation.

## Scope

The owner requested exterior construction after the included image-generation
iteration check. Start with the temple frontage, adjoining street, existing
entrance and at least one tree, using the selected temple and characters as the
scale/material benchmark. Establish reusable ground and depth-ordered exterior
layers against current authored geography. Preserve the temporary-HUD removal,
direct movement, resident controls, authoritative occupancy and deadlines.

Source generation uses the included built-in image tool with project-owned
references. Keep original outputs, preparation scripts and per-file provenance
in an external candidate packet. A model version is not inferred from quality.
Artwork-master acceptance and hosted activation are separate owner decisions.

## Implementation and proof plan

Introduce one current pixel packet shape for exterior textures and calibrated
isolated scenery. Retire the prior shape atomically, updating all loaders,
fixtures, pins and native proofs. Use shared cell projection for terrain,
entrances, characters, depth and pointer inversion. Camera motion must follow
the presented player without moving independent coordinate systems.

Search `pixelFiles`, `PixelManifest`, `schema_version`, `pixelReceipt`, exterior
projection literals and `path-controls-proof` across browser, deployment and
proof callers before the cutover. Tests cover malformed packet refusal,
geometry, camera/pointer agreement and scenery depth; native Chromium, Firefox
and WebKit runs cover exterior walking, temple entry/return, resizing and
occlusion. Inspect the runner's changed-path plan before verification.

Work begins on main at `10a8800869a03ede0441b34a6e054d2fb499cbdb` with extensive
existing uncommitted town, server and pixel work. Preserve those changes. No Git
lifecycle, public publication or hosted deployment is part of this slice.

## Delivered candidate

The current packet carries the unchanged selected interior and characters,
three reusable ground textures, a calibrated asymmetric temple facade and an
isolated tree. The manifest and every PNG are digest-bound; deployment staging
admitted 51 files from the matching packet. Version 3 replaces version 2 without
a compatibility reader. The working product remains Canvas 2D pixel art.

The browser owns a static authored-ground cache, shared moving projection,
depth ordering and alpha-aware scenery picking. Grid and movement remain
server-observed. The facade's door aligns with the existing entrance; the
tree's root sits at the building's outer corner, leaving the existing route
centres and temple transitions intact. Other buildings retain unfinished map
footprints. No geography, gameplay, character gait or temporary UI is changed.

Included image generation created the original facade, tree, paving, grass,
water and composed frontage reference. The first facade and attempted alpha
edit painted checker backgrounds. A subsequent asymmetric facade matched the
authored door offset; recorded deterministic extraction produced its reviewed
RGBA candidate without repainting its RGB. The tree had real source alpha.
Untouched originals, exact prompts, scripts, hashes, comparisons and native
captures remain in the external candidate packet. No Pixel Fixer or separately
billed provider call was made; no confirmed 2.5 model claim is made.

## Verification and findings

The selected fast runner completed all web, Python, boundary and documentation
checks, including 533 browser tests in 51 files. Exterior native input passed
Chromium, Firefox and WebKit with nine accepted commands each. The temple
regression loop also passed all three engines with 13 commands each, including
resident services, purchase, stairs, HUD absence and artwork refusal.
The final native run repeated the exterior loop after the rendering adjustment:
all three engines passed nine accepted commands, temple entry/return, compact
pointing, reconnect and missing-exterior-art refusal. Final frontage captures
were opened in all three engines; Chromium and WebKit tree-behind captures were
also reviewed. Local draw p95 was 2.3ms in Chromium, 4ms in Firefox and 26ms in
WebKit. The final selected fast runner completed every selected step again with
no unavailable result. The temporary local preview was stopped and its private
access file removed; hosted activation remains untouched.

The first native capture exposed composite terrain identities being treated as
single-layer names, causing paths to display grass. Material selection now uses
the complete authored terrain stack for scenery while gameplay still consumes
only observed legality. A subsequent static-ground cache removes artificial
observation-window holes from the backdrop without granting targets or visibility.

The initial functional exterior run recorded local WebKit draw p95 of 45ms.
An isolated draw-call profile identified redundant high-quality filtering of
already native-sized ground and scenery. Unfiltered blits of those prepared
rasters reduced the isolated profile's overall draw p95 from 48ms to 28ms;
original source reduction remains filtered. Ground draw mean fell from 29.56ms
to 8.15ms. Source cropping, smaller canvas chunks and decoded-image conversion
did not solve the problem alone; the extra conversion/chunk machinery was
removed. The packet retains before/after measurements and the profiling script.
These are local functional draw samples, not device, crowd or packaged Tauri
benchmarks.

### Remaining visual work

The presentation/browser owner retains these bounded follow-ups, with evidence
in the exterior packet's `native-final` frontage and tree-behind captures and
the composed frontage reference:

- The reusable ground kit still exposes regular grass/paving joins and a square
  pond rim. The reference has substantially better landscaping and contact.
  Next proof: an authored frontage ground/edge pass compared at the same native
  scale, preserving pond extent, door target and traversable widths.
- The frontal sanctuary artwork does not finish its deep rear footprint, which
  remains map geometry, or the remaining town buildings. Next proof: layered
  roof/side coverage from front, side and rear routes without invented collision.
- Ground raster memory and crowded-scene/device performance remain unproven.
  The current native backdrop is 3402 by 3168 pixels, about 41 MiB of RGBA storage
  before browser overhead. The browser owner must measure the intended device
  and crowd before making a wider performance claim.

These captures establish a playable exterior foundation and matching frontage
candidate, not a finished town or accepted artwork master. Whole-scene composition
must remain the visual target as the reusable layers are refined. Matching
replacement-character gaits, other interiors and dead-world art remain separate.

## Owner rejection and composition rework

The owner rejected the assembled exterior's appearance and directed use of a
full generated scene, either directly or as the controlling guide. The existing
functional proof remains valid for the recorded build; it does not qualify the
art. Rework begins from the selected interior's camera and material language.

An initial full-scene revision remained an unreviewed direction candidate.
A subsequent physical-camera blockout overemphasized roof coverage; the owner clarified that upright fronts must remain prominent,
with enough top surface to establish depth. The presentation owner records that
ruling. Neither the rejected assembly, the roof-heavy blockout nor an unreviewed
generated revision is an accepted visual target. The current runtime packet is
unchanged during this art-direction review.

The owner also required restrained image generation. The next revision reduces
scattered ground and foliage detail and gives the facade prominence over the
roof. These are whole-scene art studies, generated with the included image tool
and retained externally with exact prompts; they do not replace the runtime
packet or establish accepted masters.

## Connected nine-view study

The owner authorized the proposed 3-by-3 continuity study using the available
included image generator. Scope is one continuous town composition, nine exact
crops, a scrolling review, and source/provenance evidence. The temple lies near
the northern authored boundary, so the study follows the existing seven-building
horseshoe through the southern dock rather than inventing a northern district.

A flat guide derives positions and door sides from the compiled arrival member.
It is a land-use plan, not a roof or physical-camera guide. The quiet frontage
candidate supplies material and upright presentation direction. Generation must
retain the open central common, connected streets and seven-building roster.
No runtime geography, packet pin, movement, UI or hosted activation changes are
part of this art study.

Verification establishes exact crop coverage/reconstruction and exercises
horizontal, vertical and diagonal scrolling in all nine views. A single-source
study establishes continuity within that painting; it does not prove independent
outpainting, production-resolution detail, collision agreement or runtime
foreground layering. The tool returned a 1254-pixel square to the initial larger
resolution request, so per-view resolution must be reported from actual output.

The owner reiterated the [retained island layout](2026-09-05-first-land-surface.md#decided-by-the-owner-2026-09-05):
the rebuilt settlement is the local arrangement exception; expansion beyond it
must follow the original bitmap maps. Raw references remain external under the
[map-template ruling](../public-boundary-policy.md#first-land-map-template).
The dock opens onto the sea, with open water south of it. A generated foreground
headland was rejected and removed from the review image. This study supplies no
new island geography or independently verified bitmap correspondence.

The owner then requested a relative building/door scale audit and clarified
that undersized buildings should grow as complete structures, keeping doors,
windows, roofs and props in proportion. The presentation owner's relative-scale
ruling records the decision. An unscaled seven-door lineup exposed the smaller
bank/trade-shop entrances; a full-scene revision enlarged those buildings as
complete structures and was reviewed against the temple, lodge and training hall. The early facade-width diagram follows current
authored lots and is not acceptance of those widths as final visual proportions.
Any changed visual extents require lot/anchor reconciliation before runtime use.

The revised seven-entrance lineup was opened at a common source scale. It shows
substantially more consistent ordinary-door proportions without oversized holes
in the smaller facades; it does not claim pixel-identical openings. Original,
quieter, coastline-corrected and building-scale revisions remain in the external
packet. The final interactive study passed after the last artwork revision, including
the lossless nine-crop reconstruction check.

### Study closeout

The final source is 1254 by 1254 pixels. Its nine 418-by-418 PNG crops reconstruct
the source pixel-for-pixel, covering six horizontal joins, six vertical joins
and four four-way intersections. The inline review uses a separately recorded
WebP display copy; untouched PNGs remain the retained sources.

Chromium, Firefox and WebKit each passed selection of all nine views, diagonal
dragging, the complete joining tour, light/dark themes, compact layout and the
absence of browser exceptions. The final door lineup, Chromium diagonal join
and Firefox/WebKit overview captures were opened. Earlier Chromium desktop and
compact layouts were also visually inspected. These are art-review checks,
not a repeated live gameplay proof.

The production/browser owner retains these evidenced follow-ups:

- Produce and judge detail at actual game zoom. The requested larger generation
  yielded 1254 pixels; enlarging the current crops cannot supply missing detail.
- Reconcile larger building extents and entrance anchors against town lots and
  traversable widths before runtime promotion. The generated flat-guide match
  is composition guidance, not cell-level geographic conformance.
- Derive foreground layers and prove actor contact/occlusion in the real game.
  Independently generated extensions still need a preserved overlapping region
  and their own joining proof; exact crops alone do not prove outpainting.

The game packet, live gameplay, hosted preview and Git lifecycle remain
unchanged by this study. Source prompts, generation originals, rejected variants,
lossless crops, preparation scripts and review evidence remain external. The
selected documentation/boundary runner is the final repository check for this
art-study closeout; its observed receipt is retained with the packet.


## Pixel-fixer comparison

On September 9 the owner requested pixel correction after reviewing 1280-by-800
Steam Deck and 1920-by-1080 desktop framing, then explicitly distinguished the
standard and neural fixers. These are external art studies at the same recorded
ancestry; runtime assets, geometry and hosted activation remain unchanged.

The standard automatic full pass reduced the 1254-square source to 122-square
pixels and was rejected as too coarse. The free browser neural automatic pass
returned 317-by-243 pixels with a 24-colour palette, distorting aspect ratio.
A neural rerun at explicit 2-pixel cells and 256 colours returned 627-square
pixels but severely redistributed the scene: the temple and central common
collapsed while other buildings stretched. Both neural results are rejected.
These results establish failures on this source, not general provider quality.

Standard explicit-grid reconstruction at 627-square pixels preserved the town
composition substantially better. Both its legacy reconstruction and current
two-stage packing were retained; the latter is the diagnostic comparison source.
The before/after close view remains visibly coarser and loses fine linework. It
is not accepted as finished pixel art or a production master. The current
reconstruction also preserves many source colours rather than enforcing a small
output palette. The source-resolution follow-up above remains open: production
owns higher-detail scene art and native pixel-cluster review against this
composition before any promotion.

Six environment-only PNG comparisons cover temple frontage, southern shops and
the sea dock at both requested display dimensions. Deck exports use exact 4-times
enlargement; desktop exports use 6-times, with 320-by-200 and 320-by-180 native
crops respectively. Array comparisons prove every enlarged pixel block uniform.
This is raster evidence, not a chosen game viewport policy, character-scale
acceptance, Steam Deck hardware performance or live gameplay proof. The temple
before/after comparison and all untouched fixer outputs were opened for review.

External receipts retain source and result hashes, settings, scripts, rejected
outputs and browser captures. The standard tool was built from a pinned upstream
revision; a local Linux memory-reporting correction and a small explicit-grid
entry point leave its reconstruction algorithm unchanged. Browser automation's
initial case-sensitive status wait failed and was corrected before the successful
neural download; no timed-out capture is counted as successful evidence.


## Detailed frontage source trial

The owner authorized a higher-detail local source after the whole-town fixer
comparison. The trial takes a 640-by-400 crop, bounds (307, 0) to (947, 400),
from the retained district composition. It covers the complete temple frontage,
left pond, right tree, approach and street, and the clipped neighbouring bank
and training hall. This is one neighborhood art trial, not a replacement of the
nine-view district or its authored geography.

Included built-in image generation reconstructed that crop with finer pixel
forms. The requested 2560-by-1600 output actually arrived at 1586-by-992; a second
focused edit reduced busy grass and foliage texture. Both untouched sources and
exact prompts remain external. No model identity is inferred. Standard automatic
detection again chose an unsuitable grid, 70 by 43. An explicit 640-by-400 grid
through the standard fixer's unchanged two-stage packing produced the reviewed
candidate: the roof, stone courses, lanterns and doorway retain substantially
more readable detail than the previous district-source conversion.

The neural automatic and 256-colour versions both returned 557-by-348 pixels.
They retain the image aspect closely but alter local proportions, including the
temple width and pond/tree distribution; neither is selected for this comparison.
This is another source-specific result, not a general claim that standard
correction outperforms neural reconstruction.

The candidate has exact 1280-by-800 Deck output at 2-times nearest-neighbour
scale. Desktop output crops 20 native rows from each vertical edge and enlarges
the resulting 640-by-360 view 3-times to 1920 by 1080. Pixel-array checks prove
uniform 2-by-2 and 3-by-3 blocks. Both native-sized outputs and a before/after
frontage detail sheet were opened. These are environment-only raster reviews;
there is no character, collision, live browser or physical Deck performance
claim. The static PNG outputs require no changed browser implementation.

At this trial's closeout the source and prepared candidate awaited owner visual
review; the subsequent selection and extension are recorded below. The
production/browser owner also retains exact adjoining-edge correspondence,
building/door anchor reconciliation and foreground layer proof before runtime
promotion. The re-created source preserves the overall composition visually;
its prompted invariants do not prove unchanged pixels or seamless insertion into
the earlier town painting. All outputs, rejected variants, preparation scripts,
hashes and local/free-provider receipts remain in the external trial packet.
The selected documentation/boundary runner closes out the repository changes.


## Connected standard-correction study

The owner selected the standard-corrected frontage as the continuation reference,
retired further neural attempts and dispatched the rest of the connected town.
After a brief owner-run generation comparison, work resumed with the included
built-in tool, model unreported. The two host-only comparison images were visible
in conversation but unavailable as local source files; neither contributes to
this assembly. The study retains the same recorded ancestry and external-only
scope. Runtime assets, authored geography and hosted preview are unchanged.

Eight 640-square guides cover the surrounding sections of the continuous
1254-square composition, with origins at 0, 307 and 614 and 333-pixel overlaps.
The selected frontage supplies the northern middle. Each new source requested
2560-square pixels and actually returned 1254-square pixels. The exact guide,
frontage style reference, prompt, untouched output and generation path are
retained in the external connected-town packet. Standard correction prepares
each new source to an explicit 640-square logical grid.

The first correction introduced visible periodic grid lines. Investigation
reproduced a defect in the pinned upstream standard packer: left/top-edge cell
assignment disagreed with centre-based triangular weights. At 1254-to-640
sampling, 318 horizontal weights were negative, with a minimum near -0.507.
A deterministic test whose source channels stay within 100 to 120 produced
9,904 out-of-range output channels, reaching both 0 and 255. The local correction
assigns sample centres to cells on both axes. It reduces those violations to
zero and passes constant-colour, nonnegative-weight and exact integer-source
recovery checks. The original binary, source, failed tiles, focused patch and
reproduction script remain external. This continuation uses a documented local
correction to the standard algorithm, not unchanged upstream code.

All eight tiles were rerun. Bounded minimum-error cuts select existing pixels
through overlapping ground and scenery; no seam feathering or semantic code
painting is used. Cut paths and a diagnostic overlay are retained. The assembled
1254-square candidate preserves the selected temple core exactly at bounds
(493, 0) to (758, 310). Nine 418-square crops reconstruct it byte-for-byte.
These facts prove the assembled crop set and retained temple region; the eight
independently generated overlap regions were not themselves identical.

The complete town, seven entrances at equal native scale, Deck lodge/dock views
and desktop eastern shops were inspected. Facades remain camera-presenting,
entrances retain comparable human scale, the common stays quiet and sea extends
south of the dock without an opposite bank. The cut selection removes obvious
patch boundaries in the reviewed views. Owner acceptance of the whole district
remains open; drawing density still varies between upper forest and common.
Presentation/production owns any further aesthetic adjustment against this
retained overview and native location captures.

Four locations have full-colour Deck exports at exact 2-times enlargement of
640-by-400 native crops, and desktop exports at exact 3-times enlargement of
640-by-360 crops. Pixel arrays prove all eight exports. The compact scrolling
review uses a labelled, non-dithered 256-colour display copy; native PNGs retain
the full-colour result. Chromium, Firefox and WebKit passed both framings across
all nine views, diagonal dragging, a tour of horizontal/vertical/diagonal joins,
light/dark appearance and compact layout without page errors. Each browser also
loaded the full-colour source: all six exported native canvases equal their
corresponding integer-enlarged source crops exactly. The chat may resize the
embedded canvas to fit; this does not claim physical Deck performance.

Production/browser retains the next runtime proof: reconcile all seven entrance
anchors and building extents against authored cells, extract foreground layers,
and demonstrate character contact, occlusion and travel in the real game.
Outside the rebuilt town, the island must still follow the owner's external BMP
geography; this study did not inspect or extend that larger map. Source receipts,
sampling reproduction, assembly scripts, hashes, review captures and verification
results remain in the external packet. No neural service, separately billed API,
deployment or Git lifecycle action was used.

## Gameplay integration and pixel effects

**Owner direction, September 9:** integrate the connected town into gameplay and
apply the standard pixel treatment to the temple. After clarification, the owner
explicitly accepted fitting town geometry to the artwork. The
[surface decision](2026-09-05-first-land-surface.md#pixel-town-geography-amendment)
owns that bounded amendment; [land provenance](../../content/lands/first-expedition/README.md#september-9-pixel-town-encoding)
owns the encoded master/review/geographic hashes. The exterior geometry compiles
with 680 passable cells, 107 route cells, 196 footprint cells, seven buildings and
all sixteen directed edges. The eight interior Tiled files and service identities
are unchanged. The seed arrives on the dock at `(8,34)`; sea remains south of it.

The external `pixel-gameplay-20260909-r1` packet retains the connected 1254-square
colour scene, twelve foreground cutouts, explicit mask polygons, geometric review,
independent canonical record and compiler projection. Version 4 binds the artwork,
current geography, seven door calibrations, room patches and effect data together.
The [browser owner](../browser-client.md#pixel-art-presentation) records the actual
renderer, camera, sampling and atmosphere contracts.

### Temple and character corrections

The initial standard temple conversion to 512 square retained too much of the
old painting's grain. A subsequent 256-square reconstruction, doubled by nearest
sampling, looked overly coarse and mushy; the owner rejected it. The replacement
uses the connected exterior as the style reference while preserving the room's
functions and approaches. The owner preferred its cleaner clusters, then selected
muted grey limestone over the yellow floor. The northern basin is intentionally
read as a wall-mounted shelf. The owner questioned the paired stairwell doors,
then directed a compact cellar hatch with only a few steps fading into darkness
and a lid proportioned to cover the opening. Successive untouched sources and
rejected samplings are retained. The current prepared room is 512 square, using
the locally corrected standard two-stage packer; the 256-square trial is not used.
The stair service/landing coordinates and dungeon connectivity remain unchanged.

An integration regression initially reduced characters to the scenery's coarser
native grid with nearest sampling. That removed half their established raster
budget and damaged faces. The owner caught it; the renderer now retains high-quality
filtered preparation at the finer displayed sprite resolution. Scenery still
uses integer scaling. The Martial Artist, final Tomas and Maude standing assets
were audited against the selected `tomas-20260908-r5` packet: every standing frame
and calibration is identical. An old comparison capture was briefly shown without
clear labelling; it was historical evidence, not the active character selection.

The owner also identified the Martial Artist's finer anatomical rendering as a
style mismatch beside Tomas and Maude. One built-in front-facing simplification
candidate was generated with both as references. It initially remained separate
because its output was RGB with a painted checker. The owner then identified the
old detailed figure still present in the live capture and directed carrying the
simplified design into the game. An eight-facing turnaround used that candidate
as its reference. Deterministic matte masks retain the generated figure RGB,
followed by proportional preparation to 256-square transparent sprites with
244-pixel body height. All eight facings were inspected on light and dark fields;
Tomas and Maude remain unchanged. The complete replacement standing set also owns
the held travel poses, so turning or moving cannot restore the old detailed art.
A proper walking gait remains open, as before. A technical pixel-grid assertion
cannot establish style coherence.

### Atmosphere and lighting

The owner explicitly reaffirmed the full pixel-effects direction from the
[Graveyard Keeper developer breakdown](https://www.gamedeveloper.com/programming/graveyard-keeper-how-the-graphics-effects-are-made),
including lighting. The [presentation ruling](../presentation-direction.md#pixel-atmosphere-and-shader-effects)
owns that direction. The initial implementation has authored height/normal/foliage/
emitter maps, local light and shadow anchors, colour lookup tables, ambient review
profiles, selective leaf movement, low fog and rain. Static tree anchors and masks
are retained; chopping remains explicitly far outside this slice and would require
clean ground, removable art and authoritative state.

The first isolated three-engine shader run proved moving foliage stayed inside
its mask, scenery pixels repeated exactly at 2x, high surfaces emerged from fog,
front lighting exceeded back lighting, and outdoor rain profiles left indoor
rendering unchanged. It exposed rare rain fragments when rain strength was zero:
`step(0,0)` admits an exact-zero noise sample. An explicit positive-strength guard
fixed the cause; the final run reported zero changes outside the foliage mask.
Shader surfaces exercise the actual GPU program and maps, not gameplay transport.

### Verification and remaining work

The selected repository fast runner passed every selected meta, documentation,
boundary, Rust, Workbench, Python and web step in 171.218s, including 535 browser
tests in 52 files. The renderer/receipt/document follow-up also passed all selected
steps in 36.636s. The authoring compiler's check passed all three lands; eight
companion maps remained byte-identical; release staging verified all 66 referenced
packet files. The final temple and exterior matrices passed all three browsers. Receipts
belong to the external packet's
`temple-soft-final`, `exterior-soft-final`, `effects-final` and repository-verification output.
The live proofs use disposable PostgreSQL-backed authority and normal browser
input. The initial town loop completed entry/return for all seven buildings; its
later tree check incorrectly targeted a blocked building cell and was corrected.
The earlier corner walk also taught the proof to constrain command endpoints to
currently observed squares, even when its authored route continues behind a wall.
Neither proof may invent observation from the static artwork.

The live dock sampling proof exposed 35 channel differences at ground-grid line
endpoints. A scene-only shader fixture did not reproduce them: the mismatch was
Canvas antialiasing on the finer character raster. Ground ink now rasterizes at
the scenery resolution before nearest enlargement. The shader also explicitly
disables dithering. Scenery sampling proof excludes the intentionally finer actor
and contact layer; it does not weaken equality to a tolerance. The hostname
heuristic also misread the WebGL link-status constant; the existing boundary
allowlist now names that exact identifier with its non-network reason.

The compact-hatch temple run before the character follow-up passed all three
engines, with draw p95 of 8.7ms Chromium, 5ms Firefox and 77ms WebKit on the local
Intel UHD 630. WebKit remains a browser-performance finding, not a physical-device
claim. The browser proof owner must profile canvas/G-buffer transfers and GPU
composition under a controlled load and demonstrate a stable 30fps frame budget
before closing that finding. The final temple follow-up with the new Martial Artist and ground-ink fix passed
all three browsers with thirteen accepted commands each: p95 2.5ms Chromium,
5ms Firefox and 88ms WebKit. These functional observations do not close the
WebKit performance finding.
The final exterior circuits passed in Chromium, Firefox and WebKit, with 72
accepted commands each, all seven entry/return loops, foreground tree walks,
reconnect, missing-art refusal and zero 2x/3x scenery-block mismatches. Their
recorded draw p95 values were 7.1ms, 73ms, 80ms respectively. Firefox exterior performance therefore joins the open browser
performance finding. These rolling measurements include native capture and resize
activity; controlled profiling is required before attributing the cost to hardware
or declaring a stable frame rate.

Remaining presentation work: the revised Martial Artist needs native owner
review and a proper walking gait; normal maps are coarse planes rather than
finely authored material normals;
source plates still contain baked lighting/shadows; other service interiors remain
labelled maps; broader island correspondence and physical Steam Deck performance
are untested here. These belong to presentation/production and the browser proof
owners, with native visual acceptance and measured device evidence required.

No neural service, separately billed API, hosted activation, persistent-world
migration or Git lifecycle action was used. The work remains on the existing dirty
checkout from `10a8800869a03ede0441b34a6e054d2fb499cbdb`; pre-existing edits are retained.

The subsequent [performance slice](2026-09-09-pixel-performance.md) owns the
controlled diagnosis and repair of the browser-performance findings above.
